name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  NODE_OPTIONS: '--max-old-space-size=4096'

jobs:
  build-windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache-dependency-path: go.sum
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.14.0
      - name: Install NSIS
        run: choco install nsis -y --no-progress
      - name: Build
        env:
          PATH: ${{ env.ProgramFiles }}\NSIS;%USERPROFILE%\go\bin;${{ env.PATH }}
        run: wails build -trimpath -ldflags "-s -w" -nsis
      - uses: actions/upload-artifact@v4
        with:
          name: gitlimp-windows
          path: |
            build/bin/gitlimp.exe
            build/bin/gitlimp-amd64-installer.exe

  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install deps
        run: sudo apt-get update && sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev build-essential pkg-config
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache-dependency-path: go.sum
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.14.0
      - name: Build
        run: wails build -trimpath -ldflags "-s -w" -tags webkit2_41
      - name: Build .deb
        run: |
          mkdir -p build/linux/deb/usr/bin
          cp build/bin/gitlimp build/linux/deb/usr/bin/gitlimp
          chmod 755 build/linux/deb/usr/bin/gitlimp
          chmod 644 build/linux/deb/DEBIAN/control
          dpkg-deb --build --root-owner-group build/linux/deb gitlimp-0.1.0-amd64.deb
      - name: Build AppImage
        run: |
          wget -q https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage -O appimagetool
          chmod +x appimagetool
          ./appimagetool --appimage-extract > /dev/null
          mkdir -p AppDir/usr/bin AppDir/usr/share/applications AppDir/usr/share/icons/hicolor/512x512/apps
          cp build/bin/gitlimp AppDir/usr/bin/
          cp build/linux/deb/usr/share/applications/gitlimp.desktop AppDir/usr/share/applications/
          cp build/linux/deb/usr/share/icons/hicolor/512x512/apps/gitlimp.png AppDir/usr/share/icons/hicolor/512x512/apps/
          printf '#!/bin/sh\nSELF=$(readlink -f "%s")\nexec "$SELF" --appimage-extract-and-run\n' "$0" > AppDir/AppRun
          chmod +x AppDir/AppRun
          ./squashfs-root/AppRun AppDir gitlimp-0.1.0-x86_64.AppImage
      - uses: actions/upload-artifact@v4
        with:
          name: gitlimp-linux
          path: |
            gitlimp-0.1.0-amd64.deb
            gitlimp-0.1.0-x86_64.AppImage
            build/bin/gitlimp

  build-rpm:
    name: Build RPM (Fedora)
    runs-on: ubuntu-latest
    container:
      image: fedora:latest
    steps:
      - uses: actions/checkout@v4
      - name: Install deps
        run: dnf install -y gcc gtk3-devel webkit2gtk4.1-devel pkgconfig golang nodejs npm rpm-build git
      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.14.0
      - name: Build RPM
        run: |
          export PATH=$PATH:$(go env GOPATH)/bin
          mkdir -p ~/rpmbuild/{SPECS,SOURCES,BUILD,RPMS,SRPMS}
          tar --exclude='.git' --exclude='node_modules' -czf ~/rpmbuild/SOURCES/gitlimp-0.1.0.tar.gz .
          cp build/linux/gitlimp.spec ~/rpmbuild/SPECS/
          rpmbuild -ba ~/rpmbuild/SPECS/gitlimp.spec
      - uses: actions/upload-artifact@v4
        with:
          name: gitlimp-rpm
          path: |
            /root/rpmbuild/RPMS/**/*.rpm

  build-flatpak:
    name: Build Flatpak
    needs: build-linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: gitlimp-linux
          path: artifact
      - name: Install flatpak-builder
        run: |
          sudo apt-get update
          sudo apt-get install -y flatpak flatpak-builder
          flatpak remote-add --if-not-exists --user flathub https://flathub.org/repo/flathub.flatpakrepo
      - name: Prepare sources
        run: |
          mkdir -p flatpak-src
          cp artifact/build/bin/gitlimp flatpak-src/
          cp build/linux/flatpak/gitlimp.png flatpak-src/
          cp build/linux/flatpak/gitlimp.desktop flatpak-src/
      - name: Build Flatpak
        run: |
          flatpak install -y --user flathub org.gnome.Platform//48 org.gnome.Sdk//48
          flatpak-builder --user --force-clean --repo=repo flatpak-build build/linux/io.github.velo4705.gitlimp.json --source-dir=flatpak-src
          flatpak build-bundle --user repo gitlimp-0.1.0-x86_64.flatpak io.github.velo4705.gitlimp
      - uses: actions/upload-artifact@v4
        with:
          name: gitlimp-flatpak
          path: gitlimp-0.1.0-x86_64.flatpak

  release:
    name: Publish Release
    if: startsWith(github.ref, 'refs/tags/')
    needs: [build-windows, build-linux, build-rpm, build-flatpak]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Publish to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
